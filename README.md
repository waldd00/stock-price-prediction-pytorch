# stock-price-prediction-pytorch
Next-day stock price prediction with RNN/LSTM/GRU in PyTorch — predicts log-returns (not raw price) to avoid trend extrapolation bias, with a Streamlit demo app for any ticker via yfinance.
